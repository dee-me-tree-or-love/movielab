movies = {
      # A dict defining the actual data structure being handled by the resource. 
    # Enables data validation. 
    'schema': {
        'title': {
            'type': 'string',
            'required': True
        },
        'description': {
            'type': 'string',
        }
    }
}